spring.application.name=sakhistore

# ====================================================
# ? SERVER CONFIGURATION
# ====================================================
#Server.Servlet.Context-path=/api/v1

server.port=8080



# ====================================================
# ?? DATABASE CONFIGURATION (PostgreSQL)
# ====================================================

spring.datasource.url=jdbc:postgresql://localhost:5432/newdb
spring.datasource.username=postgres
spring.datasource.password=Thynktech
spring.datasource.driver-class-name=org.postgresql.Driver



#razorpay

razorpay.key=rzp_test_HkFY3rSv7zcCnX
razorpay.secret=oJlYLpC1hknO5cVqym5ZnNFI

# ====================================================
# ? JPA / HIBERNATE CONFIGURATION
# ====================================================

Automatically update tables based on entities
spring.jpa.hibernate.ddl-auto=update

Show SQL in console
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# PostgreSQL dialect
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# ====================================================
# ? CONNECTION POOL (Optional Optimization)
# ====================================================

spring.datasource.hikari.maximum-pool-size=10
spring.datasource.hikari.minimum-idle=2
spring.datasource.hikari.idle-timeout=30000
spring.datasource.hikari.pool-name=HikariPool

# ====================================================
# ?? LOGGING
# ====================================================
logging.level.org.hibernate.SQL=DEBUG
logging.level.org.hibernate.type.descriptor.sql.BasicBinder=TRACE

#Cloudinary Properties

cloudinary.api.name=dgrxmavho

cloudinary.api.key=733189736643147

cloudinary.api.secret=BIaTjViHLyGSiszZCH0R2rqKyRU

# ======================
# Spring security User (DISABLED - No authentication required)
# ======================

# spring.security.user.name=admin
# spring.security.user.password={noop}Thynktech_sakhistore@123
# spring.security.user.roles=ADMIN

# ======================
# JWT CONFIGURATION
# ======================

app.jwt.secret=your_very_strong_secret_key_here

app.jwt.expiration=31536000000

# ======================
# SERVER CONFIGURATION
# ======================

# Bind to all interfaces (0.0.0.0) so Android emulator can access via 10.0.2.2

server.address=0.0.0.0

# ======================
# FILE UPLOAD CONFIGURATION
# ======================

# Maximum file size (10MB - matches frontend validation)
spring.servlet.multipart.max-file-size=10MB

# Maximum request size (allows multiple files in one request)
spring.servlet.multipart.max-request-size=20MB

# Enable multipart uploads
spring.servlet.multipart.enabled=true

# ======================
# SMS CONFIGURATION (KUTILITY)
# ======================

# Development mode - set to true until Kutility credentials arrive

# When true: OTP will be printed to console (backend terminal)

# When false: OTP will be sent via Kutility SMS API

sms.dev.mode=true

sms.enabled=true

# Kutility SMS Settings (leave empty for now, fill when credentials arrive)

# To switch to Kutility SMS:

# 1. Get API credentials from Kutility

# 2. Fill in sms.kutility.api.key and sms.kutility.api.secret below

# 3. Set sms.dev.mode=false

# 4. Restart backend server

sms.kutility.api.url=https://sms.kutility.com/api/send

sms.kutility.api.key=

sms.kutility.api.secret=

sms.kutility.sender.id=SAKHI

#Spring security User
# ======================

app.store.domain=https://thynktech.com

#Email Properties
#587
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=mindrushikesh8@gmail.com
spring.mail.password=bhgc ypmv elcm hlbg
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true

# ======================
# SWAGGER/OPENAPI CONFIGURATION
# ======================

# Disable security for Swagger UI
springdoc.api-docs.path=/v3/api-docs
springdoc.swagger-ui.path=/swagger-ui.html
springdoc.swagger-ui.operationsSorter=method
springdoc.swagger-ui.tagsSorter=alpha
springdoc.swagger-ui.tryItOutEnabled=true
springdoc.swagger-ui.filter=true
springdoc.swagger-ui.display-request-duration=true
springdoc.swagger-ui.show-extensions=true
springdoc.swagger-ui.show-common-extensions=true
# Disable authentication for Swagger
springdoc.swagger-ui.enabled=true
springdoc.api-docs.enabled=true
