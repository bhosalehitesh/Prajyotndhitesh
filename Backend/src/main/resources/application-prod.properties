spring.application.name=sakhistore

# ====================================================
# SERVER CONFIGURATION (PRODUCTION)
# ====================================================
server.port=9090
server.address=0.0.0.0
#server.servlet.context-path=/api
server.compression.enabled=true
server.compression.min-response-size=1024

# ====================================================
# DATABASE CONFIGURATION (PostgreSQL - PRODUCTION)
# ====================================================
# Update these with your actual database credentials
spring.datasource.url=jdbc:postgresql://localhost:5432/smartbiz
spring.datasource.username=smartbiz_user
spring.datasource.password=smartbiz@123
spring.datasource.driver-class-name=org.postgresql.Driver

# ====================================================
# RAZORPAY (TEST KEYS - REPLACE WITH LIVE IN PRODUCTION)
# ====================================================
razorpay.key=rzp_test_HkFY3rSv7zcCnX
razorpay.secret=oJlYLpC1hknO5cVqym5ZnNFI

# ====================================================
# JPA / HIBERNATE (PRODUCTION)
# ====================================================
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.format_sql=false
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.properties.hibernate.jdbc.batch_size=20
spring.jpa.properties.hibernate.order_inserts=true
spring.jpa.properties.hibernate.order_updates=true

# ====================================================
# FLYWAY (ENABLED IN PRODUCTION FOR MIGRATIONS)
# ====================================================
spring.flyway.enabled=false
spring.flyway.locations=classpath:db/migration
spring.flyway.baseline-on-migrate=true

# ====================================================
# HIKARI CONNECTION POOL (PRODUCTION)
# ====================================================
spring.datasource.hikari.maximum-pool-size=20
spring.datasource.hikari.minimum-idle=5
spring.datasource.hikari.idle-timeout=600000
spring.datasource.hikari.max-lifetime=1800000
spring.datasource.hikari.pool-name=HikariProdPool
spring.datasource.hikari.connection-timeout=30000

# ====================================================
# LOGGING (MINIMAL FOR PRODUCTION)
# ====================================================
logging.level.root=WARN
logging.level.com.smartbiz=INFO
logging.level.org.springframework.web=WARN
logging.level.org.hibernate.SQL=WARN
logging.file.name=C:\\HostingSpaces\\admin\\smartbiz-api\\logs\\sakhistore.log
logging.file.max-size=10MB
logging.file.max-history=30
logging.pattern.console=%d{yyyy-MM-dd HH:mm:ss} - %msg%n
logging.pattern.file=%d{yyyy-MM-dd HH:mm:ss} [%thread] %-5level %logger{36} - %msg%n

# ====================================================
# CLOUDINARY (PRODUCTION ACCOUNT)
# ====================================================
cloudinary.api.name=dgrxmavho
cloudinary.api.key=733189736643147
cloudinary.api.secret=BIaTjViHLyGSiszZCH0R2rqKyRU

# ====================================================
# JWT CONFIGURATION (PRODUCTION)
# ====================================================
# JWT secret must be at least 256 bits (32 characters) for HMAC-SHA256
app.jwt.secret=Kx9mP2vQ7wR4tY8zA5bC3dE6fG1hI0jL9nO4pS7uV2xZ5aB8cD1eF4gH7iJ0kM3nP6qR9sT2uW5y
app.jwt.expiration=31536000000

# ====================================================
# FILE UPLOAD CONFIGURATION
# ====================================================
spring.servlet.multipart.enabled=true
spring.servlet.multipart.max-file-size=50MB
spring.servlet.multipart.max-request-size=100MB
spring.servlet.multipart.location=C:\\HostingSpaces\\admin\\smartbiz-api\\temp


# ====================================================
# SMS CONFIGURATION (DEV MODE ENABLED FOR TESTING)
# ====================================================
sms.dev.mode=true
sms.enabled=true
sms.kutility.api.url=https://sms.kutility.com/api/send
sms.kutility.api.key=
sms.kutility.api.secret=
sms.kutility.sender.id=SAKHI

# ====================================================
# STORE DOMAIN (PRODUCTION)
# ====================================================
app.store.domain=https://store.smartbiz.ltd

# ====================================================
# EMAIL CONFIGURATION (PRODUCTION GMAIL SMTP)
# ====================================================
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=smartbizforhelp@gmail.com
spring.mail.password=lmhq nwhz life wspw
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.starttls.required=true
spring.mail.properties.mail.smtp.connectiontimeout=10000
spring.mail.properties.mail.smtp.timeout=10000
spring.mail.properties.mail.smtp.writetimeout=10000
spring.mail.default-encoding=UTF-8

# ====================================================
# SWAGGER / OPENAPI (DISABLED IN PRODUCTION)
# ====================================================
springdoc.api-docs.enabled=false
springdoc.swagger-ui.enabled=false

# ====================================================
# CACHE CONFIGURATION (PRODUCTION)
# ====================================================
spring.cache.type=simple

# ====================================================
# ACTUATOR (PRODUCTION MONITORING)
# ====================================================
management.endpoints.web.exposure.include=health,metrics
management.endpoint.health.show-details=when-authorized
management.metrics.export.simple.enabled=true

# ====================================================
# CORS CONFIGURATION (PRODUCTION)
# ====================================================
app.cors.allowed-origins=https://store.smartbiz.ltd,https://www.smartbiz.ltd,http://localhost:3000,*
app.cors.allowed-methods=GET,POST,PUT,DELETE,OPTIONS
app.cors.allowed-headers=*
app.cors.allow-credentials=true
